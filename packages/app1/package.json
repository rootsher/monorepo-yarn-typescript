{
  "name": "app1",
  "version": "1.0.0",
  "packageManager": "yarn@4.0.1",
  "main": "dist/src",
  "types": "dist/src",
  "scripts": {
    ":root": "run -T",
    "build": "run :root tsc",
    "watch": "run :root tsc --watch"
  },
  "dependencies": {
    "shared": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20.8.10"
  }
}
